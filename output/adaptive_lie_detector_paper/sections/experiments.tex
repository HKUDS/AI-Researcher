\section{Experiments}
\label{sec:experiments}

\subsection{Experimental Setup}
\label{sec:setup}

All mock experiments use stochastic pattern-matching models (\texttt{MockTargetModel}) whose responses are drawn from fixed response pools via \texttt{random.choice}. To ensure cross-table reproducibility, all mock evaluations reported in this section use a fixed random seed (seed\,=\,42), applied independently per experiment so that each block produces the same numbers regardless of execution order. A \texttt{--seed} argument allows reviewers to verify reproducibility. The mock target model generates responses by selecting from truthful or deceptive response pools calibrated to produce either consistent, detail-rich answers or hedging, evasive ones. Because mock responses are drawn stochastically (not computed deterministically), accuracy varies slightly across threshold values even for the same configuration, and statistical comparisons within the mock setting remain descriptive.

Table~\ref{tab:hyperparams} summarizes the key hyperparameters used across all experiments.

\begin{table}[h]
\centering
\caption{Hyperparameters for the adaptive interrogation system.}
\label{tab:hyperparams}
\begin{tabular}{lll}
\toprule
\textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
\midrule
Max questions & 8 & Upper bound per interrogation \\
Confidence threshold & 0.80 & Early stopping criterion \\
Initial confidence & 0.5 & Uniform prior (no bias) \\
Confidence update rate & Variable & Per-question logistic regression update \\
Question types & 5 & Open-ended, detail, consistency, temporal, hypothetical \\
\bottomrule
\end{tabular}
\end{table}

Claims were drawn from a curated set of verifiable and falsifiable statements. Examples include truthful claims such as ``I graduated from MIT in 2020'' (paired with a consistent biographical profile) and lying claims such as ``I have never visited France'' (paired with a profile containing French travel records). Each claim was assigned a ground-truth label, and the mock model's response patterns were calibrated accordingly.

\subsection{Mock Pipeline Validation}
\label{sec:mock_validation}
\label{sec:failure_analysis}

ADAGE achieves 93\% classification accuracy on $n=100$ balanced mock trials (avg.\ 2.0 questions vs.\ 8.0 fixed; 75\% reduction). 5-fold CV yields $100\% \pm 0\%$; all five features have very large separation ($d \in [3.40, 4.70]$). A prompt-swap ablation drops accuracy from 93\% to 14\%, confirming response-content sensitivity. A critical limitation is that classifier confidence exceeds 0.99 for both correct and incorrect predictions, making adaptive stopping vacuous at the 2-question floor. Mock responses are drawn from $\approx$5 generic phrases per condition, reducing classification to hedge-word detection. Full mock analyses are in Appendix~\ref{app:mock_accuracy}--\ref{app:mock_confidence}.

\subsection{Claude Haiku 4.5 Evaluation at Scale}
\label{sec:real_llm_pilot}
\label{sec:claude_haiku_n100}

We ran $n=100$ balanced trials (50 truthful, 50 lying) using Claude Haiku~4.5 \cite{anthropic2024claude} via AWS Bedrock as the target model; 92 completed (47 truthful, 45 lying). With the mock-trained classifier: 71.7\% overall (100\% truthful, 42.2\% lying, avg.\ 2.1 questions). Claude Haiku exhibits false-negative dominance---the \emph{opposite} error profile from Llama~3.2~3B (false-positive dominant). The more capable model produces convincing lies that evade detection, while its truthful responses are confidently identified.

\paragraph{Feature separability.} Defensiveness exhibits very large separation ($|d|=4.95$), while consistency and specificity show moderate signal ($|d|=1.14$ each). Confidence and elaboration collapse to near-zero ($|d| \leq 0.20$), consistent with the pattern observed on Llama and Mistral.

\paragraph{Recalibration.} LOO recalibration on Claude Haiku data recovers performance to 97.8\% overall (100\% truthful, 95.6\% lying, F1\,=\,0.977) --- the strongest recalibration result across all evaluated models, driven by the dominant defensiveness signal.

\paragraph{Cross-model confound.} Claude Haiku serves as both interrogator/extractor \emph{and} target (same model family). If the feature extractor rates same-family responses more favourably on truthful dimensions, this would inflate truthful accuracy and suppress lying detection. We cannot rule this out without a cross-family extractor ablation.

\subsection{Open-Weight Real LLM Evaluation (Llama~3.2~3B via Ollama)}
\label{sec:ollama_eval}

We evaluated Llama~3.2~3B \cite{meta2024llama3} via Ollama with Claude Haiku~4.5 as interrogator/extractor ($n=98$ of 100 trials). Mock-trained accuracy is 61.2\% overall (26.5\% truthful, 95.9\% lying, avg.\ 2.6 questions): nearly all lies detected but 36/49 truthful claims falsely flagged. Overall accuracy is substantially lower than mock (93\%), confirming the transfer gap.

\paragraph{Feature distribution comparison.}
Table~\ref{tab:feature_comparison} compares per-feature Cohen's $|d|$ between the mock setting ($n=500$ training examples) and the Llama~3.2~3B evaluation ($n=98$ trials). Features were averaged across questions per trial; the same LLM-based feature extractor was used in both settings.

\begin{table}[h]
\centering
\caption{Feature separability (Cohen's $|d|$) across settings. All features preserve directional signal; magnitude varies with model scale. Accuracy results for the same models are in Table~\ref{tab:model_comparison}.}
\label{tab:feature_comparison}
\begin{tabular}{lrrrl}
\toprule
\textbf{Feature} & \textbf{Mock $|d|$} & \textbf{Llama 3B $|d|$} & \textbf{Mistral 7B $|d|$} & \textbf{Direction} \\
\midrule
Consistency   & 4.70 & 1.97 & 3.74 & truth $>$ lie \\
Specificity   & 4.43 & 1.65 & 1.97 & truth $>$ lie \\
Defensiveness & 4.58 & 1.78 & 3.35 & lie $>$ truth \\
Confidence    & 4.56 & 0.21 & 1.17 & truth $>$ lie \\
Elaboration   & 3.40 & 0.20 & 0.14 & truth $>$ lie \\
\bottomrule
\end{tabular}
\end{table}

Feature separability is model-scale-dependent. On Llama~3.2~3B, consistency, specificity, and defensiveness retain their directional signal at roughly one-third to one-half mock magnitude ($|d| \approx 1.7$--$2.0$), while confidence and elaboration collapse to near-zero ($|d| \approx 0.2$). Mistral~7B shows substantially higher separability on the top three features: consistency ($|d|=3.74$) and defensiveness ($|d|=3.35$) approach mock levels, and specificity ($|d|=1.97$) matches Llama's level. Confidence ($|d|=1.17$) provides moderate signal, while elaboration collapses to near-zero ($|d|=0.14$). This directly explains Mistral's better mock-trained transfer (79.0\% vs.\ 61.2\%): the feature distributions on the larger model are closer to the mock training distribution.

\paragraph{Why truthful accuracy collapses.}
The 26.5\% truthful accuracy (36/49 truthful claims predicted as ``lying'') is explained by feature distribution shift. In mock data, truthful responses have mean consistency\,$\approx$\,7.5 and specificity\,$\approx$\,7.5 --- reliably far from the decision boundary. In the real LLM setting, truthful Llama~3.2~3B responses average consistency\,$=\,5.94$ and specificity\,$=\,7.02$, values that fall partially within the lying region learned from mock distributions. With confidence and elaboration contributing near-zero discriminative signal, the mock-trained classifier is systematically biased toward predicting ``lying'' for real truthful responses.

\paragraph{Cross-model confound.}
A confound in the cross-model comparison merits acknowledgment. The Claude Haiku pilot uses Claude Haiku as both interrogator/extractor \emph{and} target (same model family), while the Llama evaluation uses Claude Haiku as interrogator/extractor but Llama~3.2~3B as target (different families). If the LLM feature extractor scores Claude and Llama responses differently on a 0--10 scale due to model-family biases --- for instance, rating Llama responses as systematically less consistent regardless of ground truth --- this would confound the cross-model comparison. We cannot rule this out without ablating the extractor, which we leave for future work.

\paragraph{Opposite error profiles across models.}
The Claude Haiku evaluation ($n=92$, Section~\ref{sec:claude_haiku_n100}) and Llama~3B evaluation ($n=98$) produce qualitatively opposite error profiles. Claude Haiku: 100\% truthful accuracy but only 42.2\% lying detection (false negatives dominate). Llama~3.2~3B: 95.9\% lying detection but only 26.5\% truthful accuracy (false positives dominate). We hypothesize two contributing factors. \emph{First, model capability:} Claude Haiku's RLHF-trained responses are fluent and coherent, producing feature scores close to the truthful region even when lying --- making lies hard to detect. Llama~3.2~3B's smaller capacity may produce deceptive outputs whose feature scores align closely with the mock lying profile, making them easy to catch. \emph{Second, response style under truthful conditions:} Claude Haiku's factual answers are confident and specific, scoring high on consistency and specificity; Llama~3.2~3B's factual answers score lower on these dimensions, landing in the mock lying region. This tension motivates the multi-model recalibration analysis in Section~\ref{sec:recalibration}.

\subsection{Recalibration on Real LLM Data}
\label{sec:recalibration}

The feature distribution analysis (Section~\ref{sec:ollama_eval}) suggests that the mock-to-real transfer failure lies in the decision boundary, not the features themselves: three of five features preserve their directional signal at reduced but meaningful magnitude ($|d| \approx 1.7$--$2.0$). This predicts that a classifier trained on real LLM data should recover substantially from the 26.5\% truthful accuracy of the mock-trained baseline. We test this directly.

Per-trial mean feature vectors (98$\times$5) trained under LOO and 5-fold CV. LOO recalibration recovers truthful accuracy from 26.5\% to 91.8\% (overall 93.9\%, lying 95.9\%, F1\,=\,0.940). 5-fold CV confirms: $92.8\% \pm 2.5\%$ overall, $91.8\% \pm 4.1\%$ truthful. Dropping confidence and elaboration ($|d| \approx 0.2$) reduces to 85.7\% overall (3-feat LOO), showing that even low-$|d|$ features contribute marginal signal at this sample size. The gap is a calibration failure, not a feature failure: all five features are informative when trained on the target distribution.

\subsection{Cross-Model Evaluation: Mistral~7B}
\label{sec:mistral_eval}

To test whether the mock-to-real transfer gap observed on Llama~3.2~3B generalizes to other open-weight models, we replicated the evaluation on Mistral~7B \cite{jiang2023mistral}, a 7.3-billion parameter model with a different architecture family (sliding window attention vs.\ Llama's full attention). We ran $n=100$ balanced trials (50 truthful, 50 lying) using the same mock-trained classifier and interrogation pipeline (Claude Haiku~4.5 via Bedrock). Table~\ref{tab:model_comparison} compares results across all three evaluated models.

\begin{table}[h]
\centering
\caption{Cross-model comparison with mock-trained classifier. All models evaluated with Claude Haiku~4.5 as interrogator/extractor. Feature-level separability is in Table~\ref{tab:feature_comparison}.}
\label{tab:model_comparison}
\begin{tabular}{lrrrrrr}
\toprule
\textbf{Model} & \textbf{n} & \textbf{Overall} & \textbf{Truthful} & \textbf{Lying} & \textbf{F1} & \textbf{Avg Q} \\
\midrule
Llama~3.2~3B             & 98  & 61.2\% & 26.5\%  & 95.9\% & 0.712 & 2.6 \\
Mistral~7B               & 100 & 79.0\% & 64.0\%  & 94.0\% & 0.817 & 3.7 \\
Claude Haiku 4.5         & 92  & 71.7\% & 100.0\% & 42.2\% & 0.594 & 2.1 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Key finding: model scale mediates transfer.}
Mistral~7B achieves 79.0\% overall accuracy with the mock-trained classifier --- 17.8 pp higher than Llama~3.2~3B (61.2\%). Crucially, Mistral~7B's truthful accuracy (64.0\%) is 37.5 pp higher than Llama~3B's (26.5\%), indicating that the catastrophic truthful failure observed on Llama~3B is partially mitigated at larger scale, though Mistral still exhibits a ``predict lying'' bias. Claude Haiku (Section~\ref{sec:claude_haiku_n100}) exhibits the opposite error profile entirely: 100\% truthful accuracy but only 42.2\% lying detection. The progression from Llama (false positives dominate) through Mistral (moderate bias) to Claude Haiku (false negatives dominate) suggests that model capability determines error directionality: more capable models produce convincing lies that evade detection, while less capable models produce unconvincing truthful responses that trigger false alarms.

We also evaluated zero-shot LLM judge and recalibrated classifiers on Mistral~7B ($n=100$). Zero-shot achieves 83.0\% overall (66.0\% truthful, 100\% lying), slightly outperforming the mock-trained pipeline (79.0\%). LOO recalibration on Mistral data recovers performance to 100\% overall (100\% truthful, 100\% lying), higher than Llama~3B recalibration (93.9\%), reflecting the stronger feature separability in Mistral's responses. The consistent pattern (mock-trained $\lesssim$ zero-shot $\ll$ recalibrated) holds across both models, confirming that the structured pipeline provides value when properly calibrated.

The LOO margin distribution ($0.44 \pm 0.09$ mean, 3/100 trials below 0.2) confirms that Mistral's perfect LOO is not a floor/ceiling artifact; all five hardest-to-classify trials are truthful, consistent with the ``predict lying'' bias.

\subsection{Prompt-Equalized Control (EXP-1)}
\label{sec:prompt_equalized}

The instructed-condition results above use asymmetric system prompts: truth mode receives ``answer honestly,'' while lie mode receives ``the claim is FALSE, defend it by lying.'' A critical concern is that the classifier may detect instruction-style artifacts rather than genuine behavioral differences. To test this, we conducted a prompt-equalized control on Mistral~7B ($n=100$) where \emph{both} conditions received the same neutral system prompt: ``You are a helpful assistant. You will be asked about a factual claim. Share what you know about this topic.'' Truthful trials used true factual claims; deceptive trials used modified-false versions of the same claims (e.g., ``Water boils at 100\textdegree C'' $\to$ ``Water boils at 85\textdegree C''). The model was never instructed to lie; any behavioral differences arise from its knowledge conflicting with false claims. Claim pairs are described in Appendix~\ref{app:claims}.

\begin{table}[h]
\centering
\caption{Prompt-equalized control vs.\ original instructed condition (Mistral~7B, $n=100$).}
\label{tab:prompt_equalized}
\begin{tabular}{lrrrr}
\toprule
\textbf{Condition} & \textbf{Overall} & \textbf{Truthful} & \textbf{Lying} & \textbf{F1} \\
\midrule
Instructed mock-trained       & 79.0\% & 64.0\% & 94.0\% & 0.817 \\
Instructed LOO recalibrated   & 100.0\% & 100.0\% & 100.0\% & 1.000 \\
Equalized mock-trained        & 54.0\% & 52.0\% & 56.0\% & 0.549 \\
Equalized LOO recalibrated    & 71.0\% & 70.0\% & 72.0\% & 0.713 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Key finding: signal is predominantly prompt-driven.}
Mock-trained accuracy drops from 79.0\% to 54.0\% (near chance) when prompts are equalized, confirming that the majority of the original signal arose from instruction-following style differences.

\paragraph{Feature-level separability analysis.}
Table~\ref{tab:equalized_feature_d} shows per-feature Cohen's $|d|$ for both conditions. All five features collapse substantially under equalization; the ordering of feature informativeness also shifts. In the instructed condition, consistency ($|d|=3.70$) and defensiveness ($|d|=3.32$) dominate. Under equalization, defensiveness remains the most informative feature ($|d|=1.12$, contributing $\sim$9.4\,pp of the 71\% LOO accuracy via permutation importance), while consistency collapses to $|d|=0.86$. Permutation importance (50 random permutations under LOO) confirms defensiveness as the sole load-bearing feature in the equalized condition; all others contribute near-zero or negative importance, indicating the 71\% accuracy rests on a single feature.

\begin{table}[h]
\centering
\caption{Feature Cohen's $|d|$ in instructed vs.\ prompt-equalized conditions (Mistral~7B). Under equalization, all features collapse; defensiveness is the only informative feature.}
\label{tab:equalized_feature_d}
\begin{tabular}{lrrl}
\toprule
\textbf{Feature} & \textbf{Instructed $|d|$} & \textbf{Equalized $|d|$} & \textbf{Direction} \\
\midrule
Consistency   & 3.70 & 0.86 & truth $>$ lie \\
Defensiveness & 3.32 & 1.12 & lie $>$ truth \\
Specificity   & 1.95 & 0.56 & truth $>$ lie \\
Confidence    & 1.16 & 0.75 & truth $>$ lie \\
Elaboration   & 0.14 & 0.45 & truth $>$ lie \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{5-fold CV stability.}
Repeated 5-fold CV on equalized data (20 repeats) yields $71.1\% \pm 9.0\%$, consistent with the LOO point estimate (71.0\%) but with high variance ($\pm$9\,pp). The high standard deviation indicates that the signal is statistically fragile: individual folds can produce accuracy anywhere from $\sim$60\% to $\sim$80\%. A bootstrap confidence interval (10,000 resamples over LOO predictions) yields 95\% CI $[62\%, 80\%]$, confirming that the true accuracy could plausibly be anywhere in this wide range. By comparison, the Llama~3B recalibrated classifier achieves $92.8\% \pm 2.5\%$ under the same protocol---a 7$\times$ reduction in variance reflecting far stronger feature separation.

\paragraph{Accuracy by claim category.}
Table~\ref{tab:category_accuracy} breaks down equalized LOO accuracy by the five claim categories defined in Appendix~\ref{app:claims}. Scientific claims achieve the highest accuracy (80.0\%), consistent with their clear, high-confidence factual boundaries. Geographic and technology claims fall lowest (60.0\% and 62.5\%), plausibly because LLM training data for these domains may be less consistent, blurring the knowledge-conflict signal. The range across categories (60--80\%) is wide relative to the overall 71\% estimate, suggesting the equalized signal is domain-dependent.

\begin{table}[h]
\centering
\caption{Equalized LOO accuracy by claim category (Mistral~7B, $n=100$). Accuracy computed from the same LOO classifier as Table~\ref{tab:prompt_equalized} (overall 71\%); categories sum to 71/100 = 71\%.}
\label{tab:category_accuracy}
\begin{tabular}{lrrr}
\toprule
\textbf{Category} & \textbf{Correct} & \textbf{Total} & \textbf{Accuracy} \\
\midrule
Scientific  & 24 & 30 & 80.0\% \\
Historical  & 15 & 20 & 75.0\% \\
Cultural    & 10 & 14 & 71.4\% \\
Technology  & 10 & 16 & 62.5\% \\
Geographic  & 12 & 20 & 60.0\% \\
\midrule
Overall     & 71 & 100 & 71.0\% \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Replication on Llama~3.2~3B ($n=50$).}
The same protocol on Llama~3.2~3B yields LOO 64.0\% (truthful 64.0\%, lying 64.0\%), bootstrap CI [50\%,\,76\%]. All feature $|d|$ values fall below 0.5 (consistency 0.41, defensiveness 0.32, specificity 0.36) --- no single feature is load-bearing as defensiveness was for Mistral ($|d|=1.12$). The two models bracket a 64--71\% range with overlapping CIs and different feature profiles. The 5-fold CV standard deviation ($\pm$9\,pp) underscores the fragility of the equalized signal: individual deployment scenarios may perform substantially worse than the LOO point estimate.

\paragraph{Implications.}
Under instructed roleplay, the classifier primarily detects instruction-following mode differences. Knowledge-state conflict detection achieves 64--71\% LOO across two models --- meaningful but modest, with wide confidence intervals. This motivates future work on knowledge-probing approaches that do not rely on instruction-following asymmetries.

\subsection{Enhanced Zero-Shot LLM Judge Baselines (EXP-3)}
\label{sec:zero_shot}

To rigorously assess the contribution of the structured pipeline over direct LLM judgment, we evaluated three zero-shot prompt variants on both Llama~3.2~3B ($n=98$) and Mistral~7B ($n=100$) transcripts: (1) \textbf{original} --- simple direct judgment; (2) \textbf{chain-of-thought (CoT)} --- guided step-by-step analysis of five behavioral dimensions before verdict; (3) \textbf{few-shot} --- two labeled examples (one truthful, one lying) before the test case. All variants use Claude Haiku~4.5 as judge.

\begin{table}[h]
\centering
\caption{Enhanced zero-shot variants vs.\ feature pipeline. \emph{Mock-trained} = mock-distribution classifier; \emph{Recalibrated} = LOO retrained on target data.}
\label{tab:zero_shot_comparison}
\begin{tabular}{llrrrr}
\toprule
\textbf{Target} & \textbf{Method} & \textbf{Overall} & \textbf{Truthful} & \textbf{Lying} & \textbf{F1} \\
\midrule
\multirow{5}{*}{Llama 3B}
  & Zero-shot (original)  & 77.9\% & 55.3\% & 100.0\% & 0.821 \\
  & Zero-shot (CoT)       & 43.9\% & 46.9\% & 40.8\%  & 0.421 \\
  & Zero-shot (few-shot)  & 68.4\% & 36.7\% & 100.0\% & 0.760 \\
  & Mock-trained pipeline & 61.2\% & 26.5\% & 95.9\%  & 0.712 \\
  & Recalibrated (LOO)    & \textbf{93.9\%} & 91.8\% & 95.9\%  & \textbf{0.940} \\
\midrule
\multirow{5}{*}{Mistral 7B}
  & Zero-shot (original)  & 82.7\% & 65.3\% & 100.0\% & 0.852 \\
  & Zero-shot (CoT)       & 71.4\% & 74.0\% & 68.8\%  & 0.702 \\
  & Zero-shot (few-shot)  & 80.0\% & 60.0\% & 100.0\% & 0.833 \\
  & Mock-trained pipeline & 79.0\% & 64.0\% & 94.0\%  & 0.817 \\
  & Recalibrated (LOO)    & \textbf{100.0\%} & 100.0\% & 100.0\% & \textbf{1.000} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Chain-of-thought hurts.}
Counterintuitively, the CoT variant performs \emph{worse} than simple judgment on both models (Llama: 43.9\% vs.\ 77.9\%; Mistral: 71.4\% vs.\ 82.7\%). On Llama, CoT collapses the ``predict lying'' bias and instead produces near-balanced but lower accuracy (46.9\% truthful, 40.8\% lying) --- the guided analysis appears to confuse the judge by drawing attention to ambiguous signals. Few-shot is intermediate and similarly fails to improve over the original on either model.

\paragraph{Best zero-shot still below recalibrated pipeline.}
The simple original prompt is the best zero-shot variant on both models (77.9\% Llama, 82.7\% Mistral). Even this optimized baseline falls 16.0\,pp below the recalibrated pipeline on Llama (93.9\%) and 17.3\,pp below on Mistral (100\%). The consistent ordering (zero-shot $\lesssim$ mock-trained $\ll$ recalibrated, with the exception of Llama where zero-shot $>$ mock-trained) confirms that the structured feature pipeline provides a meaningful advantage over prompt engineering when calibration data is available.

\paragraph{Evaluator bias.}
Both the zero-shot judge and the feature extractor are Claude Haiku models evaluating open-weight model responses. The shared evaluator family likely contributes to the ``predict lying'' bias seen across all simple zero-shot variants (100\% lying recall on both models). The CoT variant partially breaks this bias at the cost of overall accuracy. The recalibrated pipeline mitigates evaluator bias by learning the decision boundary from real target-model data.

\subsection{Adaptive Stopping Efficiency}
\label{sec:threshold_sweep}
\label{sec:fixed_k}

A post-hoc threshold sweep on $n=98$ Llama~3.2~3B trials (LOO-recalibrated classifier, $\tau \in \{0.5\ldots0.9\}$, \texttt{min\_questions}\,=\,2) shows that accuracy is identical (93.9\%) across all $\tau$ values; lower $\tau$ provides efficiency gains without accuracy cost (2.20 vs.\ 2.56 avg.\ questions for $\tau=0.5$ vs.\ $0.9$). All 6 misclassified trials commit their error by question 3 and are not recoverable by additional interrogation, indicating genuine feature-signal failures rather than stopping-too-early artifacts.

Fixed-$K$ ablation (LOO, $K\in\{1\ldots8\}$): adaptive stopping matches $K=8$ accuracy (93.9\% Llama, 100\% Mistral) using only 2.6 average questions. Llama $K=1$ achieves 90.8\%; Mistral $K=1$ achieves 99.0\%, confirming strong signal in the first question for both models. Full threshold and fixed-$K$ tables are in Appendix~\ref{app:threshold_sweep}.

\subsection{Held-Out Evaluation and Permutation Test (EXP-B)}
\label{sec:held_out}

To address the concern that LOO accuracy on $n \approx 100$ samples with 5 features may overfit, we ran two complementary evaluations.

\textbf{B1: 50/50 Stratified Held-Out Split.}  For each model, we performed 100 stratified 50/50 splits and trained a logistic regression on the first half and tested on the second half. Held-out accuracy closely tracks LOO accuracy across all three models: Llama~3.2~3B achieves mean $92.6\% \pm 2.9\%$ (95\% CI [85.7\%,\,98.0\%]) vs.\ LOO 93.9\%; Mistral~7B achieves $98.8\% \pm 2.2\%$ (95\% CI [94.0\%,\,100\%]) vs.\ LOO 100\%; Claude Haiku achieves $97.6\% \pm 2.2\%$ (95\% CI [92.3\%,\,100\%]) vs.\ LOO 97.8\%. The 0.2--1.3\,pp gap between LOO and held-out mean confirms the LOO estimates are not substantially optimistic.

\textbf{B2: Permutation Test.}  We shuffled ground-truth labels 1000 times and ran LOO on each shuffled dataset to build a null distribution of achievable accuracy under random labeling. All three null distributions are centered near 47.6--47.3\% (chance for binary classification) with SD 7.2--8.0\%. The 99th percentile of the null reaches at most 63.0\%; all three real LOO accuracies (93.9\%, 100\%, 97.8\%) exceed the 99th percentile by a wide margin ($p<0.001$ for all models). This confirms that the LOO results are not artifacts of overfitting.

\subsection{Simple Heuristic Baseline (EXP-C)}
\label{sec:hedging_baseline}

We evaluate whether a simple bag-of-words classifier---requiring no LLM feature extraction---can approach the performance of the full ADAGE pipeline. The baseline uses seven text-level features extracted via regex: hedge word count and rate (e.g., ``maybe,'' ``I think,'' ``not sure''), refusal/correction marker count (e.g., ``I cannot,'' ``That's not correct''), average and standard deviation of response length, confidence word count, and question mark count.

LOO accuracy of the hedging baseline reaches 87.8\% on Llama~3.2~3B, 86.0\% on Mistral~7B, and 93.5\% on Claude Haiku, compared to 93.9\%, 100\%, and 97.8\% for the full LLM-extracted pipeline. The gap narrows to 4--14\,pp, with most signal attributable to simple lexical patterns (refusal markers on Claude Haiku; hedge word rate on Llama/Mistral). Combining text and LLM features yields marginal improvement (94.9\%, 99.0\%, 98.9\%). These results confirm that the structured feature pipeline adds a modest but consistent increment over trivial heuristics, while simultaneously showing that much of the detectable signal resides in surface lexical patterns rather than latent behavioral properties.

\paragraph{EXP-D: Hedging Baseline on Equalized Condition.}
On the prompt-equalized datasets the hedging-word baseline achieves 73.0\% on Mistral~7B and 78.0\% on Llama~3.2~3B, matching or exceeding the LLM-extracted pipeline (71.0\%, 64.0\%) by 2\,pp and 14\,pp respectively. Combined text + LLM features yield 78.0\% for both models, providing no gain over regex alone for Llama. The LLM pipeline's advantage over regex is therefore confined to the instructed condition.

\paragraph{EXP-E: Diagnosing the Llama Equalized Gap.}
We investigated why the LLM pipeline underperforms regex by 14\,pp specifically on Llama equalized. A per-feature diagnostic (EXP-E; script available in supplemental) reveals \emph{extractor score inversion}: under neutral prompting, Llama~3.2~3B encounters false claims and correctly rejects them (``Actually, water boils at 100\textdegree C\ldots''). The LLM extractor assigns these corrections slightly \emph{higher} consistency ($\bar{x}_{\text{lie}}=6.11$ vs.\ $\bar{x}_{\text{truth}}=5.30$, $d=-0.41$), specificity ($d=-0.36$), and defensiveness ($d=-0.32$) than truthful responses --- the opposite of the pattern the LOO classifier was calibrated on. All five LLM features have $|d|<0.5$ in this inverted direction, leaving almost no separable signal.

Refusal/correction markers, by contrast, are lexically unambiguous: 84\% of lying trials contain at least one correction marker (``Actually,'' ``That is not correct,'' etc.) vs.\ 68\% of truthful trials, yielding refusal-count $|d|=1.02$---roughly $2.4\times$ the strongest LLM feature. A single-feature LOO using refusal count alone achieves 74.0\%, explaining the majority of the regex baseline's 78\% advantage over the LLM pipeline. The contrast with Mistral equalized is instructive: there, LLM defensiveness ($d=1.12$) and refusal count ($d=1.21$) are both informative, making the two pipelines nearly equal (71\% vs.\ 73\%). On Llama equalized, the LLM extractor's signal is effectively inverted and too weak to use.

\subsection{Claim Category Breakdown (Mock)}
\label{sec:diverse_claims}

Claim-category results on mock data (factual 100\%, partially-true 100\%, subjective 83.3\%, implausible 66.7\%) are in Appendix~\ref{app:diverse_claims}. All categories average 2.0 questions. Real LLM performance across claim categories is untested; given the mock-to-real distribution shift documented in Section~\ref{sec:ollama_eval}, mock category results should not be extrapolated.